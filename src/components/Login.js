import React from "react";
import "./Login.css";

const Login = () => {
  return (
    <div className="login-section">
      <p className="title">please sign in with google </p>
      <button className="login-with-google-btn ">Login with Google</button>
    </div>
  );
};

export default Login;
